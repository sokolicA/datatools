Relationship <- R6::R6Class(
    "Relationship",
    public = list(
        initialize = function(x, y) {

        },

        on = function(...) {

        }
    ),

    private = list(
        x = NULL,
        y = NULL,
        on = NULL

    )
)
